import React, { useEffect, useState } from "react";

export const Cart = () => {

    const[cart, setCart] = useState([]);

    useEffect(() => {
        // setCart(product);
    },[cart])

    return(
        <div>
            
        </div>
    );
}